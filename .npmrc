# openapi-typescript 7.13.0 declares TypeScript 5 only, while this project has
# verified its generation CLI with TypeScript 6. Remove once the peer range is
# updated upstream or the pinned generator is upgraded.
force=true
