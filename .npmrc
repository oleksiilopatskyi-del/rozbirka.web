# WARNING: force applies repo-wide to every npm command and peer dependency.
# Dependency health is checked separately while openapi-typescript 7.13.0's
# TypeScript 5 peer range is verified against this project's TypeScript 6.
# Remove when the pinned generator's peer range supports TypeScript 6.
force=true
